<%= simple_form_for @plant do |f| %>
<%= f.input :name %>
<%= f.submit "add a name", class: "btn btn-primary" %>
<%= f.input :description %>
<%= f.submit "add a description", class: "btn btn-primary" %>
<%= f.input :photo %>
<%= f.input :photo_cache, as: :hidden %>
<% end %>
